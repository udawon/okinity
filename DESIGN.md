# OKINITY — Design System (현행)

> 시네마틱 오션. 스크롤할수록 바다 깊이 내려간다.

**Theme:** dark (딥네이비 오션) · **최종 확정:** 2026-06-04 팔레트, 2026-05-31 OceanHome 승격 · **본 문서 갱신:** 2026-07-22

이 문서는 실제 렌더링 기준의 현행 시스템이다. 이전 버전(Miti Navi 양피지 테마)은 초기 탐색 단계의 레퍼런스로, 현 사이트와 무관하여 폐기했다.

## 컨셉

홈은 "수면 → 심해"로 내려가는 시네마틱 몰입형(OceanHome). 배경은 깊이별 영상/틴트 크로스페이드(OceanBackground), 콘텐츠는 다크 글래스 카드. 서브 페이지는 정적 오션 앰비언트(그라데이션+빛기둥+코스틱+공기방울).

## 색 — 포카리 스웨트 팔레트 (2026-06-04 확정)

| 역할 | 값 | 비고 |
|------|-----|------|
| 브랜드 스카이블루 | `#5fc6ef` | 주 악센트. 달력 투어 표시·PADI 카테고리 등 |
| 딥네이비 배경 | `#061522` | 기본 배경. 보조: `#06202f`, `#02101e`, `#0e2c46` |
| 쿨화이트 | `#ffffff` + 투명도 스케일 | 텍스트: 100%/90%/70%/65%/45% 단계 사용 |
| CTA 앰버 | `#fbbf24` 계열 | 예약 CTA·특별 일정(`#f2c879`) |
| 카테고리 악센트 | 스노클링 `#93c5fd` · 다이빙 `#3b82f6` · PADI `#5fc6ef` · 낚시 `#f6a623` · 요트 `#d4b878` | 투어 분류 구분용 |
| 예약 불가 | rose 계열 (`text-rose-200`, `bg-rose-400/20`) | 달력 휴무·차단 표시 |

**금지: 터콰이즈/틸 계열(`#2dd4bf` 등) 사용 금지** — 2026-06-04 팔레트 결정. 재도입하지 않는다.

## 타이포그래피

| 역할 | 폰트 | 토큰 |
|------|------|------|
| 디스플레이·헤딩(라틴) | Playfair Display 400/500/700 | `--font-playfair` |
| 세리프(한글 헤딩) | Noto Serif KR | `--font-noto-serif` |
| 본문 | Noto Sans KR | `--font-noto-sans` |
| 손글씨(히어로 연출) | Nanum Pen Script | `--font-nanum-pen` |

**스케일(실측):** H1 72px/500 → H2 48px/400 → H3 24px/400 → 카드 소제목 16px/600 → 본문 14~16px → 캡션·eyebrow **최소 12px**(`text-xs`, 11px 이하 금지) + uppercase + tracking-wider.

## 간격·형태

- 간격 스케일: Tailwind 기본(4px 베이스)
- 라운드: 카드 `rounded-card`(글래스 카드), 버튼 `rounded-button`/`rounded-full`
- 카드: `border-white/10` + `bg-white/[0.05]` + `backdrop-blur` (다크 글래스)
- 컨테이너: `max-w-container` 중앙 정렬

## 모션

- 스크롤 리빌(R 컴포넌트) — 섹션 진입 시 페이드+슬라이드, 지연 스태거
- 배경 깊이 전환 — 1200ms opacity 크로스페이드
- 호버 — 이미지 scale 1.05 / 700ms, 화살표 translate
- `prefers-reduced-motion` 존중(useStableReducedMotion). 배경 영상은 무음 장식 루프라 재생 유지

## 성능 규칙 (2026-07-22)

- 배경 영상은 SSR HTML에 넣지 않는다 — 클라이언트 판정 후 도달 깊이까지만 점진 마운트
- 모바일(<768px)·데이터 절약 모드는 배경 영상 미사용(틴트+코스틱 대체)
- 업로드 이미지는 리사이즈·경량 포맷 지향(과제: 원본 JPG 직서빙 개선)

## 다국어 표기

- 언어 선택 라벨: 한국어 / English / 日本語 (언어 이름으로 통일, 코드·이름 혼용 금지)
- 운영자 입력 한국어 텍스트(일정 등)는 EN/JA에서 상태별 번역 라벨로 대체(ScheduleCalendar 참조)
