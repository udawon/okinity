# 낚시 투어 상세 — 클래스 선택 상단 재배치 및 내용 전환 구조

- 작성일: 2026-08-29
- 대상 브랜치: feat/fishing-class-selector (origin/main 기준 신규)
- 요청: 상품 페이지 진입 시 상단에서 미들클래스/럭셔리클래스를 먼저 선택하게 하고, 선택에 따라 본문 내용이 전환되도록 재설계

## 1. 현황 분석

### 1.1 현재 페이지 구조 (운영 확인: /ko/tours/trial-fishing-4h)

렌더 순서 (`src/app/[locale]/tours/[slug]/page.tsx:96-183`):

1. ← 카테고리 링크
2. 투어 사진 캐러셀 (`TourPhotoCarousel`, 공통)
3. 헤더: 킥커 / 제목 / 요약
4. 메타 행: 소요 + **가격("미들클래스 : 100,000엔 / 럭셔리클래스 : 200,000엔" 단일 문자열)** — `page.tsx:91-94`
5. 포함 사항 (공통)
6. 본문 (공통)
7. **클래스 탭 (`TourClassTabs`) — 페이지 최하단.** 미들/럭셔리 탭 전환 시 요트 사진 1장 + 스펙 설명만 바뀜 — `page.tsx:161-173`
8. 예약 문의 CTA (`/reserve?tour={slug}`)

### 1.2 데이터 소스

| 데이터 | 저장 위치 | 비고 |
|---|---|---|
| 투어 상세(summary·price·included·body·images) | site_content `tour:{slug}` (+`:en`/`:ja`) | 스키마 `src/lib/tour.ts:105-114` |
| 클래스(미들/럭셔리) 사진·스펙 | site_content `fishing_classes` (+`:en`/`:ja`) **낚시 4종 공통 단일 키** | `src/lib/tour.ts:79-98` |
| 가격 | `tour:{slug}.price` **자유 텍스트 1개** — 클래스별 분리 없음 | `src/lib/tour.ts:110` |

- 클래스 탭 노출 판정: `tourHasClasses(slug)` = fishing 카테고리 4종 전부 (`src/lib/tour.ts:67-69`)
- 예약 폼(`src/components/ReservationForm.tsx:55`)에는 낚시 클래스 선택 상태(`fishingClass`)가 이미 존재하나, 상세 페이지에서 클래스 정보가 전달되지 않아 사용자가 폼에서 다시 골라야 함.
- `ReservePlanner.tsx:35`는 `searchParams.get('tour')`만 읽음 — `class` 파라미터는 없음.

### 1.3 운영 DB 현재 상태 (2026-08-29 조회)

- `fishing_classes` 존재: middle=YANMAR EX-46 사진+스펙, luxury=URATA UR-70 사진+스펙. `:en`/`:ja` 키도 존재.
- `tour:fishing-5species-6h`, `tour:overnight-fishing` 키 부재(미작성·비공개) — 이 두 페이지는 기본 정보 + 클래스 탭 + CTA만 노출 중.

## 2. 문제 정의

1. **선택 동선 역전**: 구매 결정에 가장 중요한 변수(클래스=배 등급, 가격 2배 차이)가 페이지 최하단에 있어, 방문자가 공통 설명을 다 읽은 뒤에야 클래스 개념을 만난다.
2. **가격 정보가 전환에 반응하지 않음**: 가격이 단일 문자열이라 클래스를 골라도 "내가 고른 클래스의 가격"이 강조되지 않는다.
3. **예약 단절**: 상세에서 클래스를 보고 넘어가도 예약 폼에서 클래스를 다시 선택해야 한다.

## 3. 해결 방안

> 2026-08-29 사용자 확정: **A-1 + B-1 + C-1 + D-1**. 이하 각 결정의 대안은 기각 근거 기록용.

### 결정 A — 클래스별 가격 데이터 모델 — 확정: A-1

#### A-1. `priceMiddle`/`priceLuxury` 필드 추가 (확정)
- `TourDetailSchema`에 `priceMiddle: z.string().default('')`, `priceLuxury: z.string().default('')` 추가.
- 표시 규칙: 선택 클래스의 가격이 있으면 그것을 표시, 두 필드 모두 비면 기존 `price` 문자열을 폴백으로 고정 표시(전환 없음).
- 어드민 `TourEditor`: 낚시 투어(`tourHasClasses`)일 때만 두 입력칸 추가. 기존 `price` 칸은 "클래스 없는 투어용/폴백" 안내와 함께 유지.
- 장점: DB 마이그레이션 0건(zod default), 하위호환 완전, 언어별 키(:en/:ja) 흐름 그대로 상속.
- 단점: 배포 후 어드민에서 낚시 4종 × 언어별 가격 입력이라는 운영 후속 작업 발생.
- 추천도: 상

#### A-2. 가격은 그대로 두고 사진/스펙만 전환
- 데이터 변경 0. 구현 최소.
- 단점: "선택에 따라 내용이 바뀐다"는 요구의 핵심(가격)이 빠진 반쪽 구현. 추후 재작업 확실.
- 추천도: 중

#### A-3. 기존 `price` 문자열을 정규식으로 파싱해 분리
- 자유 텍스트("미들클래스 : 100,000엔 / …") 파싱은 형식 변경에 취약, 언어별 표기 상이. **기각.**
- 추천도: 하

### 결정 B — 상태 관리/렌더링 구조 — 확정: B-1

#### B-1. 클라이언트 래퍼 + 서버 children (확정)
- 새 클라이언트 컴포넌트 `src/components/TourClassSection.tsx`가 선택 상태를 소유하고, 다음을 렌더:
  1. 클래스 선택 세그먼트(상단, 버튼에 클래스명+가격 병기)
  2. 메타 행(소요 + 선택 클래스 가격)
  3. 클래스 요트 사진+스펙(전환)
  4. `children` — 포함 사항·본문(서버 렌더 그대로 주입)
  5. 예약 CTA(`?tour={slug}&class={선택}`)
- `TourClassTabs.tsx`는 이 컴포넌트로 대체. 비낚시 투어는 기존 렌더 경로 그대로(픽셀 동일).
- 장점: 서버 컴포넌트 이점(본문 SSR) 유지, 상태 한 곳, 전환 즉시(왕복 없음).
- 단점: `page.tsx`의 낚시/비낚시 분기가 커짐(낚시일 때 메타·CTA를 래퍼 안으로 이동).
- 추천도: 상

#### B-2. URL 쿼리(`?class=`) + 서버 렌더
- 공유 가능한 URL, JS 없이 동작.
- 단점: 페이지가 `force-dynamic`이라 토글마다 서버 왕복 → 체감 느림, 스크롤 유지 처리 필요. 2옵션 토글에 과한 비용.
- 추천도: 하

### 결정 C — 진입 시 기본 선택 — 확정: C-1

#### C-1. 미들 클래스 기본 선택 (확정)
- 현 탭 기본값과 동일. 진입 즉시 완결된 화면. SSR 초기 화면 = 미들이라 깜빡임 없음.
- 단점: "선택을 먼저 하게 만든다"를 문자 그대로(강제 선택) 원했다면 미충족.
- 추천도: 상

#### C-2. 강제 선택 (선택 전 클래스 종속 영역을 안내 문구로 가림)
- 선택 의도가 명확해지고 클래스 인지율 100%.
- 단점: 클릭 1회의 마찰, 첫 화면에 가격·배 정보 공백, 이탈 리스크.
- 추천도: 중

### 결정 D — 예약 폼 클래스 연동 — 확정: D-1

#### D-1. CTA에 `&class={선택}` 전달 (확정)
- `ReservePlanner.tsx`: `searchParams.get('class')` 읽어 `initialClass`로 전달. `ReservationForm.tsx`: `fishingClass` 초기값으로 사용(낚시일 때만, 'middle'/'luxury' 외 값은 무시).
- 변경 2파일 소폭. 상세→예약 클래스 재선택 마찰 제거.
- 추천도: 상

#### D-2. 미연동 (현행 유지)
- 폼에 이미 클래스 드롭다운이 있으므로 기능 자체는 동작. 다만 재선택 마찰 유지.
- 추천도: 중

## 4. 트레이드오프 분석 (권장 조합 A-1+B-1+C-1+D-1)

- **얻는 것**: 클래스 우선 동선, 가격·사진·스펙 동시 전환, 예약까지 클래스 연속성, DB 마이그레이션 0건, 비낚시 투어 무영향.
- **치르는 것**: 어드민 가격 입력 운영 작업(4종×3언어, 미입력 동안은 기존 문자열 폴백으로 안전), `page.tsx` 낚시 분기 복잡도 증가, 클래스 종속 영역의 클라이언트 컴포넌트화(번들 소폭 증가 — 기존 TourClassTabs도 클라이언트였으므로 증가폭 미미).
- **바꾸지 않는 것(명시)**: 클래스 사진/스펙이 낚시 4종 공통(`fishing_classes` 단일 키)이라는 정책, 투어 사진 캐러셀(ko 키 단일 관리), SEO 메타데이터 생성 경로, 비공개 투어의 노출 규칙(클래스 영역은 공개 여부 무관 상시 노출 — 기존 `page.tsx:161` 주석과 동일).

## 5. 권장 사항

**A-1 + B-1 + C-1 + D-1.** 근거: 요구의 핵심이 "선택이 내용(특히 가격)을 바꾼다"이므로 가격 분리(A-1)는 필수에 가깝고, B-1은 기존 아키텍처(서버 컴포넌트 + 클라이언트 섬) 관례를 유지하는 최소 변경이며, C-1/D-1은 이탈 없이 전환 경험을 완성한다.

## 6. 구현 계획

### 6.1 단계

1. `src/lib/tour.ts` — `TourDetailSchema`에 `priceMiddle: z.string().default('')`, `priceLuxury: z.string().default('')` 추가, `emptyTourDetail()` 갱신. 헬퍼 `classPrice(detail: TourDetail, key: FishingClassKey): string` 추가 — 해당 클래스 가격 trim 값, 없으면 `''`.
2. `src/components/ReserveCta.tsx` 신규 — 예약 CTA 링크 프레젠테이션 컴포넌트(현 `page.tsx:175-182` 마크업 이동, props: `href`, `label`). 낚시(클라이언트)·비낚시(서버) 두 경로가 공용(중복 제거). `Link`는 `@/i18n/routing`에서 import(클라이언트 사용 선례: `OceanHome.tsx` 등).
3. `src/components/TourClassTabs.tsx` 삭제 → `src/components/TourClassSection.tsx` 신규(임포트는 `page.tsx` 1곳뿐임을 확인함).
   - props: `classes: TourClasses`, `prices: { middle: string; luxury: string; fallback: string }`, `duration: string`, `showMeta: boolean`, `slug: string`, `accent: string`, `labels`(기존 + duration/price 라벨), `children: ReactNode`.
   - 렌더 순서: ① 클래스 세그먼트(기존 `role=tablist` 마크업·스타일 유지, 버튼 내부 = 클래스명 + 아래 줄 작은 글씨로 해당 클래스 가격(`prices[key]` 있을 때만)) ② 메타 행(`showMeta`일 때만: 소요 + 가격, 빈 값 항목은 현행처럼 생략 — 가격 값은 `prices[선택]` 우선, 두 클래스 가격 모두 빈 값이면 `fallback` 고정 표시) ③ 클래스 요트 사진+스펙(기존 TourClassTabs 콘텐츠 영역 이식, 빈 콘텐츠 시 `classPreparing` 문구 동일) ④ `{children}` ⑤ `<ReserveCta href={/reserve?tour=${slug}&class=${선택}}>`.
   - 기본 선택 `useState<FishingClassKey>('middle')`.
4. `src/app/[locale]/tours/[slug]/page.tsx` — 낚시 분기(`showClasses`): 캐러셀·헤더는 공통 유지, 그 아래를 `TourClassSection`로 감싸고 포함 사항·본문 JSX를 children으로 전달. 메타 행·하단 CTA는 낚시 분기에서 래퍼 내부로 이동(`showMeta = showDetail`). 비낚시 분기: 기존 렌더 순서 그대로(CTA만 `ReserveCta`로 치환, 픽셀 동일).
5. `src/components/ReservePlanner.tsx` — `searchParams.get('class')` 읽어 `FISHING_CLASS_KEYS` 포함 값일 때만 `initialClass`로 전달(그 외 무시). `src/components/ReservationForm.tsx` — `initialClass?: FishingClassKey` prop 추가, 프리셋 카테고리가 fishing일 때만 `fishingClass` 초기값으로 사용. `reset()`은 기존대로 `''` 초기화.
6. `src/components/admin/TourEditor.tsx` — `tourHasClasses(slug)`일 때 기존 가격 칸 아래 "미들 클래스 가격"/"럭셔리 클래스 가격" 2필드 + 안내문("입력 시 상세 페이지 가격이 클래스 선택에 따라 전환됩니다. 비워 두면 위 가격 안내가 그대로 노출됩니다."). `saveTour` 액션은 스키마 확장만으로 통과(코드 변경 불필요 — `tour-actions.ts:38` safeParse가 신규 필드 수용).
7. i18n — **신규 키 0개** 확정: `classMiddle`/`classLuxury`/`classTitle`/`priceLabel`/`durationLabel`/`reserveCta`/`classPreparing` 모두 ko/en/ja에 존재 확인(messages/en.json:246-260, ja.json:246-). `messages/*.json` 변경 없음.
8. 검증(6.3) 후 커밋 → 운영 후속: 어드민에서 낚시 4종 × ko/en/ja 클래스별 가격 입력.

### 6.2 영향 범위

- 변경: `src/lib/tour.ts`, `src/app/[locale]/tours/[slug]/page.tsx`, `src/components/ReservePlanner.tsx`, `src/components/ReservationForm.tsx`, `src/components/admin/TourEditor.tsx`
- 신규: `src/components/TourClassSection.tsx`, `src/components/ReserveCta.tsx`
- 삭제: `src/components/TourClassTabs.tsx` (임포트 1곳 확인 — `page.tsx:23`)
- 무영향: 비낚시 투어 10종 렌더(CTA 치환은 마크업 동일), 홈, SEO 메타(`generateMetadata` 미변경), `fishing_classes` 어드민 폼(`FishingClassesForm`), `messages/*.json`, DB 스키마

### 6.2-b 변경 대상 히스토리 (git 근거)

- `TourClassTabs.tsx`: 643a1ae(클래스 탭 도입) → 253bf4f(낚시 공통 단일 키화). 도입 목적 "소분류 별도 페이지 없이 탭 노출"은 신규 컴포넌트에서도 유지(별도 페이지 안 만듦). 공통 단일 키 정책도 유지 — 원 설계 의도 훼손 없음.
- `page.tsx`: 7ae8173(다국어 언어별 키) → ba39a3d(사진 캐러셀·ko 키 단일 해석). 두 정책 모두 이번 변경에서 그대로 유지(텍스트 로케일 우선·사진 ko 단일).
- `ReservationForm.tsx`: d03de26(투어별 시간대) 등 — `fishingClass` 상태는 이미 존재, 초기값 주입만 추가라 제출 로직(`FISHING_CLASS_PRODUCT` 표기) 불변.

### 6.3 검증 계획

- `npm run typecheck` + `npm run lint` (테스트 러너 부재 — vitest 도입은 별도 과제로 분리, 이번 변경은 순수 로직을 `classPrice` 헬퍼로 고립시켜 수동 검증 범위 최소화)
- gstack 브라우저: 낚시 4종(작성 2·미작성 2) 전환 동작·가격 폴백, 비낚시 1종 회귀(픽셀 동일), 상세→예약 클래스 사전 선택, en/ja 라벨.

### 6.4 리스크

1. 가격 미입력 기간 — 폴백으로 기존 단일 문자열 노출(전환 없음). 심각도: 낮음(현행과 동일 화면).
2. `class` 쿼리에 임의 값 주입 — `FISHING_CLASS_KEYS` 화이트리스트 검증으로 차단. 심각도: 낮음.
3. 미작성 투어(5종낚시·1박2일)의 메타 없음 상태 — 현행 노출 규칙 유지로 회귀 없음 확인 필요. 심각도: 중간(QA 항목).

---

검토 상태: 개발 준비 완료 (2026-08-29 /plan-check 3차 통과 — 1차: 모호성 8건 반영, 2차: 네이밍 1건 수정(TourClassSection), 3차: 이슈 0건)
